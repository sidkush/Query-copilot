# ── Core Framework ──────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]>=0.34
pydantic==2.12.5
pydantic-settings>=2.6
python-dotenv>=1.0

# ── AI & RAG ────────────────────────────────────────────────
anthropic==0.86.0
chromadb>=1.0

# ── Database — Core ────────────────────────────────────────
sqlalchemy==2.0.48
pandas>=2.2
pyarrow>=15.0
polars>=1.0
datafusion>=43.0

# ── Task Queue ──────────────────────────────────────────────
celery>=5.3.0

# ── ML Engine ───────────────────────────────────────────────
scikit-learn>=1.4
xgboost>=2.0
lightgbm>=4.0
joblib>=1.3
statsmodels>=0.14
safetensors==0.4.5
sentence-transformers==3.3.1
rank_bm25==0.2.2

# ── Database Drivers — Relational ──────────────────────────
psycopg2-binary>=2.9              # PostgreSQL + CockroachDB
PyMySQL>=1.1                      # MySQL + MariaDB
pymssql>=2.2                      # Microsoft SQL Server
# SQLite driver is built-in to Python

# ── Database Drivers — Cloud Data Warehouses ───────────────
# snowflake-sqlalchemy            # Snowflake (install manually if needed)
# google-cloud-bigquery[sqlalchemy] # BigQuery (install manually if needed)
# sqlalchemy-redshift>=0.8        # Amazon Redshift (install manually — requires SQLAlchemy <2.0)
# redshift-connector>=2.0         # Amazon Redshift driver

# ── Database Drivers — Analytics Engines ───────────────────
duckdb>=0.9                       # DuckDB
duckdb-engine>=0.9                # DuckDB SQLAlchemy adapter
clickhouse-sqlalchemy>=0.3        # ClickHouse
clickhouse-driver>=0.2            # ClickHouse driver

# ── Database Drivers — Distributed SQL ─────────────────────
sqlalchemy-cockroachdb>=2.0       # CockroachDB
# databricks-sql-connector>=3.0  # Databricks (install manually — requires ODBC)
# databricks-sqlalchemy>=1.0     # Databricks SQLAlchemy adapter
trino>=0.320                      # Trino

# ── Database Drivers — Enterprise ──────────────────────────
oracledb>=1.0                     # Oracle
# sqlalchemy-hana>=2.0           # SAP HANA (install manually — requires hdbcli C extension)
# hdbcli                         # SAP HANA driver (proprietary, manual install)
# ibm-db-sa>=0.4                 # IBM Db2 (install manually — requires C compiler)
# ibm_db>=3.0                    # IBM Db2 driver

# ── SQL Validation ──────────────────────────────────────────
sqlglot==30.1.0

# ── Testing ─────────────────────────────────────────────────
pytest-asyncio>=0.21

# ── Auth ────────────────────────────────────────────────────
python-jose[cryptography]==3.5.0
passlib[bcrypt]>=1.7
bcrypt==5.0.0

# ── CORS & Utils ────────────────────────────────────────────
python-multipart>=0.0.18
requests>=2.31

# ── Scheduling ──────────────────────────────────────────────
apscheduler>=3.10

# ── Redis ───────────────────────────────────────────────────
redis>=5.0
google-cloud-bigquery-storage>=2.0

python-frontmatter==1.1.0
tiktoken==0.12.0

# ── VizQL protobuf codegen (Plan 7a) ────────────────────────
# protobuf is already transitively required; pin explicitly.
# grpcio-tools provides python -m grpc_tools.protoc (bundled protoc binary).
protobuf>=5.29,<7
grpcio-tools>=1.68

# ── Analytics — Trend / Forecast / Cluster ────────────────
numpy>=1.26
scipy>=1.12
