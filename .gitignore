# ── Secrets & credentials ──────────────────────────────────────
backend/.env
backend/gcp-credentials.json
backend/.data/admin_credentials.json
backend/.data/Twilio recovery code.txt

# ── User data (runtime, not source) ───────────────────────────
backend/.data/users.json
backend/.data/deleted_users.json
backend/.data/pending_verifications.json
backend/.data/oauth_states.json
backend/.data/otp_store.json
backend/.data/sent_otps.log
backend/.data/server.log
backend/.data/user_data/

# ── ChromaDB vector store (rebuilt on reconnect) ──────────────
backend/.chroma/

# ── Python bytecode ───────────────────────────────────────────
__pycache__/
*.pyc
*.pyo

# ── Node / Frontend ──────────────────────────────────────────
node_modules/
dist/
dist-ssr/
*.local
frontend/.vite/

# ── IDE & OS ─────────────────────────────────────────────────
.vscode/
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
Thumbs.db

# ── Claude Code / Superpowers local state ────────────────────
.claude/settings.local.json
.superpowers/brainstorm/*/state/

# ── Logs ─────────────────────────────────────────────────────
logs/
*.log
npm-debug.log*
yarn-debug.log*

# Git worktrees for parallel feature development
.worktrees/

# ── Agent worktrees (transient, never commit) ─────────────────
.claude/worktrees/

# Eval baseline is committed in git per H13 (eval integrity)
!.data/eval_baseline.json
# Ring-1 trap baseline — committed per H13 (eval integrity)
!.data/coverage_baseline.json
# Phase C Ring-3 trap baselines — committed per H13
!.data/name_inference_baseline.json
!.data/join_scale_baseline.json

# Phase D Ring-4 trap baseline — committed per H13
!.data/intent_drop_baseline.json

# Phase E trap baselines — committed per H13
!.data/sampling_trust_baseline.json
!.data/multi_tenant_baseline.json

# Phase F trap baseline — committed per H13
!.data/correction_pipeline_baseline.json

# Phase G retrieval-budget corpus + baseline — committed per H13
!.data/retrieval_budget_corpus.jsonl
!.data/retrieval_budget_baseline.json

# Phase H hardening-surface baseline — committed per H13
!.data/hardening_surface_baseline.json

# Phase K Ring-8 trap baselines — committed per H13
!.data/hallucination_baseline.json
!.data/budget_baseline.json
!.data/planner_baseline.json
!.data/escalation_baseline.json

# Also negate backend/.data copies (alternate path)
!backend/.data/hallucination_baseline.json
!backend/.data/budget_baseline.json
!backend/.data/planner_baseline.json
!backend/.data/escalation_baseline.json

# Phase L trap baselines — committed per H13
!.data/audit_integrity_baseline.json
!.data/claim_verification_baseline.json
!.data/plan_cache_baseline.json
!.data/cancel_propagation_baseline.json

# Phase M-alt trap baselines
!.data/transpile_regression_baseline.json
!.data/capability_routing_baseline.json
